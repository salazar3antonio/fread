<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/save_fetched_article_menu"
        android:checkable="true"
        android:checked="false"
        android:icon="@drawable/ic_bookmark_border_white_24dp"
        android:title="@string/save_article_menu_text"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/add_tags_menu_item"
        android:title="@string/add_tag_menu_text"
        app:showAsAction="never" />

</menu>